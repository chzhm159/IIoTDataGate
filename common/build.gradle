plugins {
    id 'java'
    id 'java-library'
    id 'com.github.johnrengelman.shadow' version '7.0.0'
}

group 'org.idw.common'
version '0.0.1'
archivesBaseName = 'common'

dependencies {
    api(libs.junit4)
    api(libs.bundles.logs)
    api(libs.bundles.commons)
}
