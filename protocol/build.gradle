plugins {
    id 'com.github.johnrengelman.shadow' version '7.0.0'
    id 'java-library'
}
group 'org.idw.protocol'
version '0.0.1'

archivesBaseName = 'protocol'

dependencies {
    api project(':common')
}
